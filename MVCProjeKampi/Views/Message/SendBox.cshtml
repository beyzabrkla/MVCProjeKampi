@using EntityLayer.Concrete
@model List<Message>
@{
    ViewBag.Title = "Gönderilen Mesajlar";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Gönderilen Mesajlar</h1>
            </div>
        </div>
    </div>
</section>

<section class="content">
    <div class="row">
        @Html.Action("ContactPartial", "Contact")
        <div class="col-md-9">
            <div class="card card-primary card-outline">
                <div class="card-header">
                    <h3 class="card-title">Gönderilen Mesajlar</h3>
                    <div class="card-tools">
                        @using (Html.BeginForm("SendBox", "Message", FormMethod.Get))
                        {
                            <div class="input-group input-group-sm">
                                <input type="text" name="search" class="form-control" placeholder="Mail Kutusunda Ara" value="@ViewBag.CurrentSearch">

                                <div class="input-group-append">
                                    <button type="submit" class="btn btn-primary">
                                        <i class="fas fa-search"></i>
                                    </button>
                                </div>
                            </div>
                        }
                    </div>
                </div>

                <form id="bulkActionFormSendbox" method="post" action="/Message/MessageBatchMoveToTrash">
                    <div class="card-body p-0">
                        <div class="table-responsive mailbox-messages">
                            <table class="table table-hover table-striped">
                                <thead>
                                    <tr>
                                        <th style="width: 20px;"></th>
                                        <th>Kime</th>
                                        <th>Konu</th>
                                        <th>Tarih</th>
                                        <th>İşlem</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var x in Model)
                                    {
                                        <tr>
                                            <td>
                                                <div class="icheck-primary">
                                                    <input type="checkbox" name="selectedMessages" value="@x.MessageId" id="check@(x.MessageId)">
                                                    <label for="check@(x.MessageId)"></label>
                                                </div>
                                            </td>
                                            <td>
                                                <a href="/Message/GetSendBoxMessageDetails/@x.MessageId">@x.ReceiverMail</a>
                                            </td>
                                            <td>
                                                <a href="/Message/GetSendBoxMessageDetails/@x.MessageId">@x.Subject</a>
                                            </td>
                                            <td>@x.MessageDate.ToString("dd MMM yyyy HH:mm")</td>
                                            <td>
                                                <a href="/Message/MessageMoveToTrash/@x.MessageId" class="btn btn-default btn-sm"
                                                                                          title="Çöp Kutusuna Taşı"
                                                                                          onclick="return confirm('Bu mesajı çöp kutusuna taşımak istediğinizden emin misiniz?')">
                                                    <i class="far fa-trash-alt"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <div class="card-footer p-0">
                        <div class="mailbox-controls">
                            <button type="button" class="btn btn-default btn-sm checkbox-toggle-sendbox">
                                <i class="far fa-square"></i>
                            </button>
                            <div class="btn-group">
                                <button type="button" class="btn btn-default btn-sm" id="bulkTrashBtnSendbox">
                                    <i class="far fa-trash-alt"></i>
                                </button>
                            </div>
                            <a href="/Message/SendBox" class="btn btn-default btn-sm">
                                <i class="fas fa-sync-alt"></i>
                            </a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>

<script src="~/AdminLTE/plugins/jquery/jquery.min.js"></script>
<script>
    $(document).ready(function () {
        // Mailbox Seçimini Aç/Kapa
        $('.checkbox-toggle-sendbox').on('click', function () {
            var checked = $('.mailbox-messages input[type="checkbox"]').first().prop('checked');
            $('.mailbox-messages input[type="checkbox"]').prop('checked', !checked);
        });

        // Toplu Çöp Kutusuna Taşıma İşlemi
        $('#bulkTrashBtnSendbox').on('click', function () {
            if ($('.mailbox-messages input[type="checkbox"]:checked').length > 0) {
                if (confirm('Seçili mesajları çöp kutusuna taşımak istediğinizden emin misiniz?')) {
                    $('#bulkActionFormSendbox').submit();
                }
            } else {
                alert('Lütfen en az bir mesaj seçin.');
            }
        });
    });
</script>